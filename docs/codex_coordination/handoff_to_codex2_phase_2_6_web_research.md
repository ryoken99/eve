# Handoff To Codex 2: Phase 2.6 Web Research Preparation

Generated by: Codex 1 on PC1

Purpose: deliver the PC2 order through GitHub because RustDesk input was not reliable.

## Required Branch

Codex 2 should create and publish:

```text
codex2/prepare-real-web-research
```

## Objective

Prepare Eve for the first real public web research test, limited to one item, without executing real web browsing yet.

This is a preparation and validation phase only.

## Do Not Do Yet

- Do not execute real web research.
- Do not open browser for real web collection.
- Do not log in anywhere.
- Do not use accounts.
- Do not publish online.
- Do not send email.
- Do not use X/Twitter.
- Do not use admin privileges.
- Do not run self-update.
- Do not promote lab candidates.
- Do not alter real cron jobs.
- Do not commit runtime/private memory/log/state files.

## Implementation Scope

Review and prepare `research/daily_research_runner.py` so `run_daily_research_collection()` explicitly supports:

- `dry_run`
- `max_items`
- `real_web`
- `allowed_domains`
- `no_login`
- `no_account_actions`

Expected behavior:

1. `dry_run=True` must not perform real web access.
2. `dry_run=False, real_web=False` must preserve current/local behavior.
3. `dry_run=False, real_web=True` may be implemented as a safe mock/stub in this branch, but must not perform real web access yet.
4. Real web execution must remain blocked until Codex 1 audits the branch and Sandro/ChatGPT explicitly authorizes Phase 2.6B.

## Safety Rails

The implementation must explicitly preserve or enforce:

- no login
- no account actions
- no publication
- no email/X
- no suspicious downloads
- max one item for the first real web phase
- allowed public domains only when real web is later enabled

## Required Report

Create:

```text
docs/codex_coordination/pc2_web_research_prepare_report.md
```

The report must include:

- branch name
- commit hash
- files changed
- tests run
- confirmation that no real web access happened
- confirmation that no login/accounts/publication/email/X/admin/self-update/cron changes happened
- summary of `real_web=True` behavior
- whether any mock/stub was used
- next step recommended for Codex 1 audit

## Tests

Run relevant tests, at minimum:

```powershell
python -m pytest tests
python scripts/run_capability_tests.py
```

If full tests are too slow or fail due to environment, run targeted tests and record the exact failure in the report.

## Expected Response To Codex 1

When complete, report:

- branch published
- commit hash
- files altered
- test results
- confirmation of no real web
- report path

Codex 1 will then fetch and audit `origin/codex2/prepare-real-web-research` before any real web research is authorized.
