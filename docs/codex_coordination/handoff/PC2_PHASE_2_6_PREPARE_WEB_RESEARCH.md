# PC2 Phase 2.6 Prepare Web Research

Generated by: Codex 1 on PC1

This is the GitHub handoff file requested after RustDesk input proved unreliable.

## Target Branch For PC2

```text
codex2/prepare-real-web-research
```

## PC2 Objective

Prepare Eve for limited real web research, but do not execute real web research in this phase.

This phase is preparation only.

## Required Work

Update or prepare `research/daily_research_runner.py` so `run_daily_research_collection()` supports:

- `dry_run`
- `max_items`
- `real_web`
- `allowed_domains`
- `no_login`
- `no_account_actions`

## Required Behavior

1. `dry_run=True` does not perform real web access.
2. `dry_run=False, real_web=False` keeps the existing behavior.
3. `dry_run=False, real_web=True` is prepared for limited public web research, but in this branch it must only be tested with mock/stub behavior.
4. No real web access is allowed yet.

## Explicit Safety Rules

- no login
- no accounts
- no publication
- no email/X
- no suspicious downloads
- no admin
- no self-update
- no lab promotion
- no real cron changes

## Forbidden In Commit

- logs
- state
- secrets
- workspace
- memory runtime
- transcripts
- vector DB
- private lab reports

## Required Report

Create:

```text
docs/codex_coordination/pc2_web_research_prepare_report.md
```

The report must include:

- branch
- commit hash
- files changed
- tests
- summary of `real_web=True` mode
- confirmation no real web was executed

## Expected PC2 Commit And Push

```powershell
git commit -m "Prepare limited real web research mode"
git push -u origin codex2/prepare-real-web-research
```

## Expected Reply To Sandro/Codex 1

Report:

- branch
- commit hash
- files changed
- tests
- summary of `real_web=True` mode
- confirmation no real web was executed
